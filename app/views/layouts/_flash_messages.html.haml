= flash_message(flash[:success], 'alert-success') if flash[:success]
= flash_message(flash[:error],   'alert-danger')  if flash[:error]
= flash_message(flash[:alert],   'alert-warning') if flash[:alert]
= flash_message(flash[:notice],  'alert-info')    if flash[:notice]
